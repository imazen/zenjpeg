[package]
name = "butteraugli"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Butteraugli perceptual image quality metric - Rust port from libjxl"

[features]
default = ["simd"]
simd = []

[dependencies]
# SIMD support
wide = { workspace = true }

[dev-dependencies]
# Image I/O for testing
png = { workspace = true }

# JPEG encoding/decoding for roundtrip tests
mozjpeg = { workspace = true }
jpeg-decoder = { workspace = true }

# Testing utilities
approx = { workspace = true }
