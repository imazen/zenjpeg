# Codecov configuration for zenjpeg
# https://docs.codecov.io/docs/codecov-yaml

coverage:
  precision: 2
  round: down
  range: "60...90"  # Coverage range for badge colors
  status:
    project:
      default:
        target: auto  # Use previous coverage as baseline
        threshold: 1%  # Allow 1% decrease without failing
        if_ci_failed: error
    patch:
      default:
        target: 80%  # New code should have 80%+ coverage
        threshold: 5%

# Define coverage for key codec paths
flags:
  encode:
    paths:
      - zenjpeg/src/encode.rs
      - zenjpeg/src/entropy.rs
      - zenjpeg/src/huffman.rs
      - zenjpeg/src/huffman_opt.rs
      - zenjpeg/src/dct.rs
      - zenjpeg/src/quant.rs
      - zenjpeg/src/adaptive_quant.rs
      - zenjpeg/src/color.rs
      - zenjpeg/src/xyb.rs
      - zenjpeg/src/scan_script.rs
    carryforward: true

  decode:
    paths:
      - zenjpeg/src/decode.rs
      - zenjpeg/src/idct.rs
      - zenjpeg/src/entropy.rs
      - zenjpeg/src/huffman.rs
      - zenjpeg/src/color.rs
      - zenjpeg/src/icc.rs
    carryforward: true

  core:
    paths:
      - zenjpeg/src/types.rs
      - zenjpeg/src/error.rs
      - zenjpeg/src/consts.rs
      - zenjpeg/src/bitstream.rs
    carryforward: true

# Ignore test utilities and examples from coverage requirements
ignore:
  - "zenjpeg/src/test_utils.rs"
  - "zenjpeg/examples/**"
  - "zenjpeg/benches/**"
  - "internal/**"

comment:
  layout: "header, diff, flags, files"
  behavior: default
  require_changes: true
  require_base: false
  require_head: true
  hide_project_coverage: false

parsers:
  gcov:
    branch_detection:
      conditional: true
      loop: true
      macro: false
      method: false
