[package]
name = "simd-test-standalone"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
wide = "0.7"
multiversion = "0.7"

# Portable timing that works on native and WASM
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
# Native uses std::time::Instant

[target.'cfg(target_arch = "wasm32")'.dependencies]
web-time = "1.1"
