# Issue #78 status check after rebase onto main

Branch: `investigate/q10-baseline-gap`, rebased onto main at $(git log -1 --format=%h main).

## Q10 baseline 4:2:0 size delta vs cjpegli (zen / cpp - 1)

Sample of 16 images from the verify harness:

- **Mean: +0.64%** (was ~3% in the original issue #78 measurement)
- **Max: +3.18%** (graphic/graph — a synthetic line-art screenshot)
- **Min: −0.21%** (zen now smaller than cjpegli on photographic content)
- Most photos: within ±0.5% of cjpegli; many tied or smaller.

## Conclusion

The Q10 baseline 4:2:0 size gap appears largely closed on photographic content.
A residual gap remains on synthetic / screenshot / graphic-arts content
(`frymire`, `graphic/graph`, `graphic/gui`, `graphic/codec_wiki`).

Likely contributors to the closure (commits between `c33f8821` and current main):
- XYB Option-1 SIMD kernel and related encode-side changes
- The XYB ICC handling fix landed yesterday
- Various encoder cleanup and table tweaks

Next steps: focus dering offset calibration on the screenshot/graphics
cluster only, since photos no longer need it.
